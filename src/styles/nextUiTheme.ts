import { createTheme } from "@nextui-org/react";

export const nextUiTheme = createTheme({
    type: "light",
    theme: {
        colors: {
            // brand colors
            background: '#DEDEDE',
        },
    }
})